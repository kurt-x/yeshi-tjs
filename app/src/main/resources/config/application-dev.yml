server:
  ssl:
    enabled: off

spring:
  datasource:
    url: jdbc:h2:mem:web
  data:
    redis:
      host: 192.168.2.35
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: on
    properties:
      hibernate:
        format_sql: on
  h2:
    console:
      enabled: off
